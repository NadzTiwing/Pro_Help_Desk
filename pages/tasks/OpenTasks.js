import { View, Text } from "react-native";

const OpenTasks = () => {
    return(
        <View>
            <Text>This is where the open tickets are being displayed.</Text>
        </View>
    );
}

export default OpenTasks;